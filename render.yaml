services:
  - type: web
    name: historias-clinicas-api
    env: node
    plan: free
    buildCommand: npm install --include=dev && npm run build:frontend
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000

